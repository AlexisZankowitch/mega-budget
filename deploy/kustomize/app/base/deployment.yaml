apiVersion: apps/v1
kind: Deployment
metadata:
  name: megabudget
  labels:
    app: megabudget
spec:
  replicas: 1
  selector:
    matchLabels:
      app: megabudget
  template:
    metadata:
      labels:
        app: megabudget
    spec:
      containers:
        - name: megabudget
          image: megabudget:latest
          ports:
            - name: http
              containerPort: 8080
          envFrom:
            - secretRef:
                name: app-db-secret
          env:
            - name: DATABASE_URL
              value: postgres://$(APP_DB_USER):$(APP_DB_PASSWORD)@postgres.databases.svc.cluster.local:5432/$(APP_DB_NAME)?sslmode=disable
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 2
            periodSeconds: 10
            timeoutSeconds: 2
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 2
            periodSeconds: 5
            timeoutSeconds: 2
