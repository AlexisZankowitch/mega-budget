apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: databases
nameSuffix: -dev

resources:
  - namespace.yaml
  - ../../base

configMapGenerator:
  - name: megabudget-dev-db-config
    literals:
      - APP_DB_NAME=megabudget_dev
      - APP_DB_USER=megabudget_app_dev

generatorOptions:
  disableNameSuffixHash: true

patches:
  - target:
      group: batch
      version: v1
      kind: Job
      name: db-provision
    path: patch-job.yaml
